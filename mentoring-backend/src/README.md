How to run (local)
1. Copy .env.example -> .env, set MONGO_URI to your MongoDB connection string (use MongoDB Atlas URI for production).
2. npm install
3. npm run dev (or npm start for production)
4. Backend API will be exposed on http://localhost:5001
